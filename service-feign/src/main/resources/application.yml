eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8765
spring:
  application:
    name: service-feign
## feign 中自带断路器，默认不启用，启用即可
feign:
  hystrix:
    enabled: true

